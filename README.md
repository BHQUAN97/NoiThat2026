# NoiThat2026 Backups

Daily backups. Encrypted with AES-256 if BACKUP_ENCRYPT_KEY is set.

Decrypt: gpg --decrypt --output FILE FILE.gpg

Structure: noithat/YYYY-MM-DD/{noithat2026.sql.gz, uploads.zip, settings.json}
